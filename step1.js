var election = require('./lib/electionEngine');

var candidates = [
    'Hilarious Clintface',
    'Bernus Sandface',
    'Adolph Trump'
]

var votes = [

]

// Get votes from API
var votes = 

// Calculate the winner
var winner = election.getWinner();




console.log( winner.name + ' has won the nomination with ' + winner.totalVotes + ' votes' );

